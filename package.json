{
  "name": "user-directory-api",
  "version": "1.0.0",
  "description": "This is an api that gets, stores, update and delete user/user from a directory ",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "stephen oisewemen",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-validator": "^7.3.0",
    "nodemon": "^3.1.10"
  }
}
